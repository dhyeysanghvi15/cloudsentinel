# $0 AWS bill guarantee:
# - Do not deploy to AWS.
# - This repo runs fully offline in Demo Mode (GitHub Pages) and Local Mode (localhost).

# API (FastAPI, localhost-only)
AWS_SCAN_ENABLED=false
AWS_REGION=us-east-1
DATA_DIR=./data
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
PROJECT_TAG=cloudsentinel
OWNER_TAG=dhyey
ENV=local
LOG_LEVEL=INFO
ALLOW_ADMIN_SIM=1

# Web (Next.js)
# GitHub Pages build uses NEXT_PUBLIC_BASE_PATH="/cloudsentinel"
NEXT_PUBLIC_BASE_PATH=
